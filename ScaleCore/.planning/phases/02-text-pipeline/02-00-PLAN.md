---
phase: 02-text-pipeline
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - tests/unit/buffer.test.ts
  - tests/unit/tag-check.test.ts
  - tests/unit/from-me-filter.test.ts
  - tests/unit/splitter.test.ts
  - tests/unit/memory-window.test.ts
  - tests/unit/cost-lookup.test.ts
  - tests/unit/balance-check.test.ts
autonomous: true
requirements: [PIPE-02, PIPE-03, PIPE-04, PIPE-05, MEM-02, BILL-02, BILL-03]

must_haves:
  truths:
    - "vitest is installed and configured for the project"
    - "npm test runs vitest and exits cleanly (all stubs pass or are skipped)"
    - "7 test stub files exist under tests/unit/ matching VALIDATION.md requirements"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest configuration with path aliases matching tsconfig"
      contains: "defineConfig"
    - path: "tests/unit/buffer.test.ts"
      provides: "Test stubs for PIPE-02 (buffer add/drain)"
      contains: "describe"
    - path: "tests/unit/splitter.test.ts"
      provides: "Test stubs for PIPE-05 (response splitting)"
      contains: "describe"
    - path: "tests/unit/cost-lookup.test.ts"
      provides: "Test stubs for BILL-02 (cost lookup from CURATED_MODELS)"
      contains: "describe"
  key_links:
    - from: "vitest.config.ts"
      to: "tsconfig.json"
      via: "path alias resolution for @ imports"
      pattern: "resolve.*alias"
---

<objective>
Install vitest and create 7 test stub files documented in VALIDATION.md. This is Wave 0 — all subsequent plans depend on test infrastructure being in place.

Purpose: Satisfy the Nyquist rule — every plan task must have an automated verify command that can actually run. Without vitest installed and stubs created, the `<automated>` commands in Plans 01-03 have no test runner.
Output: vitest config + 7 stub test files + npm test script
</objective>

<execution_context>
@/Users/lautreck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lautreck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-text-pipeline/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest and create configuration</name>
  <files>package.json, vitest.config.ts</files>
  <read_first>
    - package.json (check existing scripts and dependencies — NOTE: root package.json is at the monorepo/workspace root, one directory up from ScaleCore if needed)
    - tsconfig.json (check path aliases — the @ alias likely maps to ScaleCore root)
    - next.config.js or next.config.mjs or next.config.ts (check for any relevant config)
  </read_first>
  <action>
Install vitest as a dev dependency:
```bash
npm install -D vitest
```

Create `vitest.config.ts` at the project root (same level as package.json):
```typescript
import { defineConfig } from "vitest/config";
import path from "path";

export default defineConfig({
  test: {
    globals: true,
    environment: "node",
    include: ["ScaleCore/tests/**/*.test.ts"],
  },
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "ScaleCore"),
    },
  },
});
```

IMPORTANT: Adjust the `include` path and `alias` based on what you find when reading the project structure. The package.json is at `/Users/lautreck/Desktop/Trabalho/SenaWorks/package.json` (parent of ScaleCore). The @ alias should resolve to the ScaleCore directory. Check tsconfig.json for the actual alias mapping and mirror it.

Add a test script to package.json if not already present:
```json
"test": "vitest run"
```
  </action>
  <verify>
    <automated>grep -c "vitest" package.json && test -f vitest.config.ts && echo "config exists"</automated>
  </verify>
  <done>vitest installed as devDependency, vitest.config.ts created with correct path aliases, "test" script added to package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Create 7 test stub files</name>
  <files>tests/unit/buffer.test.ts, tests/unit/tag-check.test.ts, tests/unit/from-me-filter.test.ts, tests/unit/splitter.test.ts, tests/unit/memory-window.test.ts, tests/unit/cost-lookup.test.ts, tests/unit/balance-check.test.ts</files>
  <read_first>
    - .planning/phases/02-text-pipeline/02-VALIDATION.md (Wave 0 Requirements section — lists exact files and requirements each covers)
    - .planning/phases/02-text-pipeline/02-01-PLAN.md (interfaces section — get function signatures for memory, billing, openrouter modules)
    - .planning/phases/02-text-pipeline/02-02-PLAN.md (interfaces section — get function signatures for buffer, splitter modules)
  </read_first>
  <action>
Create the `tests/unit/` directory (under ScaleCore or at the workspace root — match the vitest.config.ts include path from Task 1).

Create 7 stub test files. Each stub should:
- Import `describe`, `it`, `expect` from `vitest`
- Have one `describe` block with the module name
- Have `it.todo()` stubs for each behavior to test
- Include a comment noting which requirement it covers

**File 1: `tests/unit/buffer.test.ts`** (PIPE-02)
```typescript
import { describe, it, expect } from "vitest";

describe("buffer", () => {
  it.todo("addToBuffer returns isFirst=true on first message");
  it.todo("addToBuffer returns isFirst=false on subsequent messages within window");
  it.todo("drainBuffer returns concatenated messages joined by newline");
  it.todo("drainBuffer returns null when lock not acquired");
  it.todo("drainBuffer returns null when buffer is empty");
});
```

**File 2: `tests/unit/tag-check.test.ts`** (PIPE-03)
```typescript
import { describe, it, expect } from "vitest";

describe("tag-check", () => {
  it.todo("agent processes message when chat has matching activation_tag");
  it.todo("agent skips message when chat has no matching activation_tag");
  it.todo("agent skips message when chat.tags is null");
  it.todo("agent skips message when chat.tags is empty array");
});
```

**File 3: `tests/unit/from-me-filter.test.ts`** (PIPE-04)
```typescript
import { describe, it, expect } from "vitest";

describe("from-me-filter", () => {
  it.todo("skips processing when fromMe is true");
  it.todo("processes message when fromMe is false");
  it.todo("skips processing when messageType is not text");
  it.todo("processes message when messageType is text and fromMe is false");
});
```

**File 4: `tests/unit/splitter.test.ts`** (PIPE-05)
```typescript
import { describe, it, expect } from "vitest";

describe("splitResponse", () => {
  it.todo("splits text by double newline into paragraphs");
  it.todo("splits single paragraph by sentence-ending punctuation");
  it.todo("returns original text when no split points found");
  it.todo("trims whitespace from split parts");
  it.todo("handles Portuguese punctuation correctly");
});
```

**File 5: `tests/unit/memory-window.test.ts`** (MEM-02)
```typescript
import { describe, it, expect } from "vitest";

describe("memory-window", () => {
  it.todo("getConversationHistory limits results to 50 messages by default");
  it.todo("getConversationHistory returns messages in chronological order");
  it.todo("getConversationHistory accepts custom limit parameter");
  it.todo("saveConversationMessages inserts both user and assistant messages");
});
```

**File 6: `tests/unit/cost-lookup.test.ts`** (BILL-02)
```typescript
import { describe, it, expect } from "vitest";

describe("cost-lookup", () => {
  it.todo("returns creditsPerMessage for known model ID");
  it.todo("returns default cost of 1 for unknown model ID");
  it.todo("looks up model from CURATED_MODELS by id field");
});
```

**File 7: `tests/unit/balance-check.test.ts`** (BILL-03)
```typescript
import { describe, it, expect } from "vitest";

describe("balance-check", () => {
  it.todo("returns allowed=true when wallet balance >= cost");
  it.todo("returns allowed=false when wallet balance < cost");
  it.todo("returns allowed=false when wallet not found");
  it.todo("calls deduct_wallet_balance RPC with correct args when balance sufficient");
});
```

All files use `it.todo()` so they are recognized by vitest but do not fail. Implementation plans will convert todos to real tests.
  </action>
  <verify>
    <automated>ls tests/unit/buffer.test.ts tests/unit/tag-check.test.ts tests/unit/from-me-filter.test.ts tests/unit/splitter.test.ts tests/unit/memory-window.test.ts tests/unit/cost-lookup.test.ts tests/unit/balance-check.test.ts && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>All 7 test stub files exist under tests/unit/. `npm test` (vitest run) completes without errors — all stubs are recognized as todos, none fail.</done>
</task>

</tasks>

<verification>
- `npm test` runs vitest and exits with code 0
- All 7 test files exist and are recognized by vitest
- vitest.config.ts resolves @ alias correctly
- No existing functionality broken by adding vitest
</verification>

<success_criteria>
- vitest installed and configured
- 7 stub test files created matching VALIDATION.md Wave 0 Requirements
- `npm test` passes (all stubs are todos, not failures)
- Subsequent plans can reference `npm test` in their automated verify commands
</success_criteria>

<output>
After completion, create `.planning/phases/02-text-pipeline/02-00-SUMMARY.md`
</output>
