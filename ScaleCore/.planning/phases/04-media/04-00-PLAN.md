---
phase: 04-media
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - ScaleCore/tests/unit/media-processor.test.ts
  - ScaleCore/tests/unit/media-library.test.ts
autonomous: true
requirements: [MEDIA-01, MEDIA-02, MEDIA-03, LIB-03, LIB-04, LIB-05]

must_haves:
  truths:
    - "Test stubs exist for all media processing and library behaviors"
    - "pdf-parse is installed and importable"
  artifacts:
    - path: "ScaleCore/tests/unit/media-processor.test.ts"
      provides: "Test stubs for audio, image, PDF processing"
      contains: "it.todo"
    - path: "ScaleCore/tests/unit/media-library.test.ts"
      provides: "Test stubs for prompt injection, marker extraction, marker stripping"
      contains: "it.todo"
  key_links: []
---

<objective>
Wave 0: Install pdf-parse dependency and create test stub files for all Phase 4 unit-testable behaviors.

Purpose: Nyquist compliance — every subsequent plan has test files ready for TDD or verification.
Output: 2 test stub files + pdf-parse in package.json
</objective>

<execution_context>
@/Users/lautreck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lautreck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-media/04-RESEARCH.md
@.planning/phases/04-media/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pdf-parse and create test stubs</name>
  <files>
    ScaleCore/tests/unit/media-processor.test.ts,
    ScaleCore/tests/unit/media-library.test.ts,
    package.json
  </files>
  <read_first>
    ScaleCore/tests/unit/buffer.test.ts,
    package.json
  </read_first>
  <action>
1. Run `npm install pdf-parse` from the monorepo root (`/Users/lautreck/Desktop/Trabalho/SenaWorks/`).

2. Create `ScaleCore/tests/unit/media-processor.test.ts` with the following stubs using the existing `it.todo()` pattern (match the style in `buffer.test.ts`):

```typescript
import { describe, it } from "vitest";

describe("media-processor", () => {
  describe("audio transcription (MEDIA-01)", () => {
    it.todo("builds multimodal message with input_audio content part for ogg audio");
    it.todo("falls back to text-only model description when agent model does not support audio");
    it.todo("fetches base64 from Supabase Storage URL");
  });

  describe("image description (MEDIA-02)", () => {
    it.todo("builds multimodal message with image_url content part using data URI");
    it.todo("falls back to text-only model description when agent model does not support vision");
    it.todo("includes caption text alongside image when caption is present");
  });

  describe("PDF text extraction (MEDIA-03)", () => {
    it.todo("extracts text from PDF buffer and truncates to 4000 chars");
    it.todo("returns truncated text with ellipsis indicator for long PDFs");
    it.todo("handles empty PDF gracefully");
  });
});
```

3. Create `ScaleCore/tests/unit/media-library.test.ts` with the following stubs:

```typescript
import { describe, it } from "vitest";

describe("media-library", () => {
  describe("prompt injection (LIB-03)", () => {
    it.todo("appends media catalog to system prompt when agent has media items");
    it.todo("returns base prompt unchanged when no media items");
    it.todo("only includes active media items in catalog");
    it.todo("formats each item as [MEDIA:uuid] name: description");
  });

  describe("marker extraction (LIB-04)", () => {
    it.todo("extracts single MEDIA marker UUID from AI response text");
    it.todo("extracts multiple MEDIA marker UUIDs from AI response text");
    it.todo("returns empty array when no markers present");
  });

  describe("marker stripping (LIB-05)", () => {
    it.todo("strips all MEDIA markers from text leaving clean response");
    it.todo("collapses multiple spaces after marker removal");
    it.todo("trims leading and trailing whitespace after stripping");
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/lautreck/Desktop/Trabalho/SenaWorks && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - ScaleCore/tests/unit/media-processor.test.ts contains "it.todo" and "audio transcription" and "image description" and "PDF text extraction"
    - ScaleCore/tests/unit/media-library.test.ts contains "it.todo" and "prompt injection" and "marker extraction" and "marker stripping"
    - package.json contains "pdf-parse"
    - `npx vitest run` exits 0 (all todo tests are skipped, not failing)
  </acceptance_criteria>
  <done>2 test stub files created with all planned test cases as it.todo(), pdf-parse installed, full test suite passes</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` shows media-processor and media-library test files with skipped/todo tests
- `node -e "require('pdf-parse')"` does not throw
</verification>

<success_criteria>
- pdf-parse in node_modules and package.json
- 2 test stub files matching validation strategy test map
- Existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-media/04-00-SUMMARY.md`
</output>
